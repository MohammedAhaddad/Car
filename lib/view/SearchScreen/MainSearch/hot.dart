import 'package:flutter/material.dart';

class hotMain extends StatelessWidget {
  const hotMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hot"),
    );
  }
}
