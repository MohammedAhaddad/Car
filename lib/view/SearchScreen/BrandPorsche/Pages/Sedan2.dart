import 'package:flutter/cupertino.dart';

class Sedan2 extends StatelessWidget {
  const Sedan2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Sedan2"),
    );
  }
}
